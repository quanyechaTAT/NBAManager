server:
  port: 8080

spring:
  application:
    name: nba-manager-backend
  datasource:
    url: jdbc:mysql://localhost:3306/nba_manager?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: dl041280
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: "nbamanager-demo-secret-key-at-least-256-bits-for-hs256-algorithm-xxxxxxxx"
    expiration-ms: 86400000
